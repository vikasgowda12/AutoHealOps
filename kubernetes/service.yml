apiVersion: v1
kind: Service

metadata:
  name: autohealops-service
  labels:
    app: autohealops-backend

spec:
  selector:
    app: autohealops-backend

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000

  type: LoadBalancer